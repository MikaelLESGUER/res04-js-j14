export default {
    data() {
      return {
        numbers: [12, 35, 56, 67, 42, 93]
      };
    },
    computed: {
      // sortedNumbers() {
      //   return this.numbers.slice().sort((a, b) => a - b);
      // }
    }
  }

